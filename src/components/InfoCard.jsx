export default function InfoCard() {
  return (
    <section className="card info">
      <strong>How review timing works</strong>: each completed problem enters a simplified SM-2
      review queue. Your first review is tomorrow, then after three days, with longer intervals
      for strong recall. A failed review resets the interval to 1 day. Progress stays in this
      browser and never leaves your device.
    </section>
  );
}
